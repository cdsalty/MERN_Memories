server:
- npm install body-parser cors express mongoose nodemon 

client(app):
npm install:
  - axios moment react-file-base64 redux redux-thunk @material-ui/core



- the mongoose schema is under models.postMessage